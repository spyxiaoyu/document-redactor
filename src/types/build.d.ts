// These globals are injected by Vite's `define` option at build time.
// See vite.config.ts. The values are git short hash + ISO build timestamp.
declare const __BUILD_HASH__: string;
declare const __BUILD_TIME__: string;
